# PAL CaféBot

PAL Café customer AI ordering and service bot — Khalifa City, Abu Dhabi, UAE.

## Status

Project structure only. No application code has been built yet.
Build proceeds one step at a time per the PAL CaféBot build sequence.

## Folder Structure

- `prompts/` — AI system prompt(s)
- `data/` — menu, promotions, and orders data (JSON)
- `frontend/` — customer chat UI
- `backend/` — API and order logic
- `.env.example` — placeholder environment variables (no real secrets)


ANTHROPIC_API_KEY=sk-ant-your-real-key-here
ANTHROPIC_MODEL=claude-sonnet-5
PORT=3000
